version: 0.0

phases:
        install:
                runtime-versions:
                        java: corretto11
        pre_build:
                commands:
                        - echo pre_buile...
                        - sudo apt-get install -y maven
        build:
                commands:
                        - echo build...
                        - mvn install
        post_build:
                commands:
                        - echo post_build...
artifacts:
        files:
                - target/testapp-0.1.war
